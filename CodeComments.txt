Bugs:

Double-click to open section editor, but then go select something else, without cancelling. Section editor stays open. Should close. 

Filter selection border in Section View should be a a different color than manual selection.

Restore from backup calls a new instance, associated crashes.

Flow Check:
Brand new db, no .tpconfig. Check all configs migrate properly. 
Are the backups being configured properly? Check all paths.

-validate database filename. Accepts blanks right now. Validate to Windows standards?
-BlockPatternView is very reptitive! Extract to a control.
The campus selector in Section Prefixes is not being populated?
Block patterns and campus not coming through to the db?




Agenda.

UI design --> strengthen keyboard interaction

Hover over dbnames (filenames) shows full path

Key board shortcuts

Double-click on a section editor to save???
Make save/close button larger

After a new db possibly other times, Academic Unit is default. Setting it does not revise the display 
in Schedule View. 



Wizard is duplicating section properties as they end up in the Settings?

Use a package for error logging e.g. Sentry, Firebase Crashlytics, Bugsnag

Improve human-readability of db.

Should there be an inactivation on section properties etc so they could be retired rather than deleted.

Ordering on Section Prefixes,

need a bit of margin on lefthand section list

What happens if we modify legal start times while a semester is in use?

Sort Section View by tag?



Show notes in closed card?

User can add fields to the right?

Zoom to cursor? Via scroll wheel?

Banner on sections header to show number of sections

Hover help in a lot of places